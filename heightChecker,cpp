class Solution {
public:
    int heightChecker(vector<int>& h) {
        vector<int> temp=h;
        sort(h.begin(),h.end());
        int cnt=0,n=h.size();
        for(int i=0;i<n;i++){
            if(temp[i]!=h[i])   cnt++;
        }
        return cnt;
    }
};
